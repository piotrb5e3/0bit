<h4>
  {{post.title}}
    <br>
    <small>
        Posted: <time>{{moment-calendar post.date}}</time>
        {{#if (and session.isAuthenticated (not isPreview)) }}
          {{#link-to "post.edit" post}}edit{{/link-to}}
        {{/if}}
    </small>
</h4>
{{yield}}
